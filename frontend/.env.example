# Copie para .env se precisar sobrescrever. Vars do Vite precisam do prefixo VITE_.

# URL base da API.
# Dev: deixe em branco/omitido -> usa http://localhost:3333 (ver src/lib/api.ts)
# Produção (origem única): "/" -> chamadas relativas ao mesmo domínio da SPA
VITE_API_URL="/"
